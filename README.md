# Email Sorter
Sorts emails into categories using a Naive Bayes model.
## Usage
Upon reaching the index page, log in using Google and allow the program access to modify the labels on emails (this will exclude sent and draft emails.)